export type TGameMode = "multi" | "single";
export type TGameStage = "game" | "join" | "lobby" | "selectGameMode" | "selectLesson" | "wait";
export type TPlayerRole = "master" | "slave" | "spectator";


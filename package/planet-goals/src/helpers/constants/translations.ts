export type TLocale = "el" | "en" | "es" | "it" | "nb" | "pl" | "sl" | "sv";

export enum LocalesEnum {
    el = 'el',
    en = 'en',
    es = 'es',
    it = 'it',
    nb = 'nb',
    pl = 'pl',
    sl = 'sl',
    sv = 'sv',
}